---
scope: Book List whole system — parent storefront and admin, one Node process
purpose: build-substrate for sole developer/deployer Daanlk
altitude: feature
updated: 2026-08-18T10:48
---

- (event) Create intent, Coaching path. Workspace bound: architecture-BookList-2026-08-18. Inputs: final PRD+addendum, UX EXPERIENCE.md+DESIGN.md (draft). No app code, no spec, no parent spine.
- (constraint) Inherit PRD/UX as read-only: order trap not bookstore; Express+SQLite+static public/ HTML thin starter (Next+Postgres rejected); zero outbound messaging; COD no gateway; English-only; WCAG 2.2 AA; Chalk & Brass; two interfaces storefront+admin; cart server-persisted; order snapshot total; book master shared; packs typed name computed price.
- (decision) Audience: sole developer and deployer (Daanlk). Spine is a build-substrate for one builder, not a team alignment doc. Extra human-facing architecture deck deferred unless asked at finalize. Binds: artifact size and jargon. Prevents: writing a discussion architecture for an audience that does not exist.
- (decision) Bonto is NOT a hard deploy constraint. Host is a portability target. App stays one Node process + durable SQLite file so it can move. Prevents: baking Bonto sleep/hour/RAM limits into the architecture as identity.
- (version) Deploy research 2026-08-18: Bonto free = 50h/mo FAQ (pricing also lists 75h), 0.5 CPU/512MB, 256MB storage, auto-sleep 30min, 0 always-on, no CC. Bonto Glitch = $8/mo, 1 always-on, 1 custom domain. Render free Node sleeps 15min, ephemeral FS so local SQLite is wiped on spin-down; free Postgres expires 30 days. Fly.io no free tier for new users. Railway no permanent free tier. Koyeb free Node gone (joining Mistral; paid from $29). Turso free hosted SQLite: 5GB / 500M row reads / 10M writes / 100 DBs. Oracle Always Free: 2 OCPU + 12GB ARM A1 (or 2x AMD micro 1GB), 200GB block, always-on persistent disk; CC required; idle VMs can be reclaimed (<20% CPU/net/mem over 7 days); capacity often tight.
- (decision) Deploy path ADOPTED: stay on Bonto free through build and application testing; move to Bonto Glitch ($8, 1 always-on, 1 custom domain) at go-live. Same Node+SQLite app, no rewrite. Free-tier sleep and hour cap are accepted only for pre-production. Prevents: treating a sleeping free PaaS as production; switching runtime/host class at deploy time.
- (decision) Environments: Bonto free URL is the test/staging surface the vendor may see; Glitch always-on is production. Config/secrets differ by env; code does not. Cold-start skeleton remains (UX already designed) because free-tier testing still sleeps; production on Glitch is always-on so it becomes a no-op.
- (direction) Next load-bearing call: design paradigm and process boundaries. Stack seed inherited (Express + SQLite + static public/ HTML) not reopened.
- (decision) Paradigm ADOPTED: modular monolith. One Node deployable, domain modules own their tables and writes, two static HTML shells (storefront, admin) are clients of one JSON API. Binds: process shape, module cut, API-vs-UI split. Prevents: a second service; UI-sliced modules that duplicate order/catalog rules; raw SQL or cross-UI writes around an owner.
- (decision) Module cut ADOPTED: four domain folders plus thin web. identity (accounts, sessions, recovery), catalog (schools, grades, books, packs, items), cart (saved basket), orders (placed orders and lifecycle). Storefront and admin are HTML doors, not owners. Binds: source tree, write ownership. Prevents: UI-sliced modules; cart inserting orders; catalog storing pack price.
- (decision) Divide-and-conquer implementation order is binding: identity -> catalog -> cart -> orders. Each folder is finished and tryable before the next starts. A folder is only used through its functions, never by reading another folder's tables. Web HTML for the slice being built ships with that slice (login pages with identity, browse with catalog, etc.). Prevents: building all screens first with no owner; later folders reaching backward.
- (constraint) Inherited mutation rules from PRD, not re-asked: Place Order is atomic and idempotent; order ids concurrency-safe; status transitions optimistic-concurrency with T1-T11 adjacency; confirmed orders never dereference live catalog; pack price computed not stored.
- (decision) Auth ADOPTED: httpOnly session cookie, one mechanism for parent and admin, role on the account record. Binds: identity module, all authenticated routes. Prevents: JWT/localStorage tokens; a second login scheme per UI.
- (decision) Session records live in SQLite (same DB file), not in memory. Bonto free sleep and any restart would otherwise wipe logins. Cookie flags: HttpOnly, SameSite=Lax, Secure on HTTPS. Prevents: in-memory express-session store.
- (direction) Daanlk: long-term maintainability and easy-to-read code are binding. Vanilla HTML lean is challenged; React was not offered and should have been. Next+Postgres stays rejected (fat starter); React-as-pages on Express API is a different call and is now on the table.
- (decision) Pages ADOPTED: React + Vite, not Next. Two apps (storefront, admin) plus a small shared UI kit (Chalk & Brass tokens/components). Express serves the Vite build and owns /api. Binds: client toolchain, two bundles, shared visual pieces. Prevents: vanilla HTML sprawl; Next.js; shipping admin JS to parents; a named kit (MUI/shadcn/Tailwind) that fights DESIGN.md.
- (constraint) UX DESIGN.md remains the visual contract. Shared UI is custom components + CSS variables from those tokens, not a third-party component library.
- (decision) Language ADOPTED: TypeScript on both the four server folders and the two React apps. Binds: all source. Prevents: JS/TS split that lets boundaries drift; plain JS public APIs with no compiler check.
- (version) Stack seed 2026-08-18 (web-checked): Node 22 (Bonto max LTS; 18/20 EOL or dead), Express 5.2.x, better-sqlite3 12.x/13.x not node:sqlite (experimental), React 19.2.x, Vite 8.x, TypeScript current. Dev: tsx + Vite. Prod: tsc + vite build, Express serves dist. No Next, no Prisma.
- (decision) Stack ADOPTED as seed: Node 22, Express 5, better-sqlite3, React 19, Vite 8, TypeScript. Native add-on better-sqlite3 is acceptable on Bonto Node 22. Prevents: Node 24 (not on Bonto); experimental node:sqlite; Express 4.
- (decision) API ADOPTED: same-origin JSON over /api/*, cookie credentials, no tRPC/GraphQL. Error envelope always { error: { code, message } }. Binds: both React apps, all routes. Prevents: per-app error shapes; RPC frameworks that hide URLs.
- (decision) Persistence ADOPTED: one SQLite file; each domain folder owns its queries as SQL via better-sqlite3; no ORM. Schema applied by numbered migrations in dependency order (identity, catalog, cart, orders). One connection, WAL. Prevents: Prisma/Drizzle; per-request new connections; queries against another folder's tables.
- (decision) Admin export ADOPTED: one Export action, one zip containing (1) CSV files for Gothami in Excel and (2) a copy of the SQLite file for restore. Owned by a small export function that reads through catalog and orders, never raw table dumps from web/. Binds: FR67 durability. Prevents: CSV-only (no restore) or db-only (unreadable to the vendor); two drifting export buttons.
- (decision) Conventions ADOPTED (user did not override): money as integer rupees; timestamps UTC in DB, Asia/Colombo on screen; JSON camelCase / SQL snake_case; error envelope { error: { code, message } }; integer PKs; public order number is short sequential. Source tree: server/{identity,catalog,cart,orders,db/migrations,web} + client/{storefront,admin,ui}.
- (decision) Ops ADOPTED: scrypt for passwords (no extra native addon); secrets in env vars not repo; admin recovery code generated first boot, printed once to server log, hash only in DB, rotated on use, never in the website; console logging only; no worker/queue/mailer. Place Order is one SQLite transaction plus client idempotency key; status changes compare expected current status (T7/T8). Prevents: bcrypt/argon extra addons; secrets in git; in-app recovery display; a second process.
- (event) User accepted remaining ops list. Distill starting.
- (event) Reviewer gate: lint clean. Reconcile-prd conflict; reconcile-ux conflict; rubric revise; versions pass-with-findings; adversarial revise. Applying clear AD tightenings; recording PRD/addendum/UX supersessions.
- (decision) Supersessions: AD-2 go-live on Bonto Glitch overrides PRD §9 free-tier-as-v1-production (user 2026-08-18). AD-6 React+Vite overrides addendum static public/ HTML and EXPERIENCE custom-HTML line; still one Express process, not Next. FR10 admin-resets-parent-password and FR74 recovery code stay in architecture despite EXPERIENCE parking those UX screens for v2.
- (decision) Gate tightenings: identity owns profile fields and is the only Set-Cookie source; cart pack line is cloned configuration with add-time prices for FR49; orders owns distinct notes + delivery/payable; named statuses + T2-T6 T9 in AD-9; mounts / /admin /api; web opens one DB and migrates at boot; Node engines >=22.12, Bonto runtime 22 not default 20; tsx 4.23.12.
- (event) spine finalized
